# Connect With Cursor

Cursor can use the same local mechanisms:

- MCP wrapper: `python mcp_server/sigmai_mcp_server.py`
- CLI: `python tools/sigmai.py status`
- Pairing code: `python tools/sigmai.py connect SG-4821-KQ9M`

All operations stay on the local machine.
